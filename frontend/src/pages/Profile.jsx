import React, {useEffect, useState} from "react";
import Layout from "../components/Layout"

export default function Profile(){

    return(
    <Layout pageTitle = {`Nazwa profilu`}>
        <div>Here will be the profile site</div>
    </Layout>
    )
}